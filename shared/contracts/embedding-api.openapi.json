{
  "openapi": "3.0.3",
  "info": {
    "title": "Guised Up Embedding Service",
    "version": "0.1.0",
    "description": "Placeholder OpenAPI contract — implementation pending"
  },
  "paths": {
    "/embed": {
      "post": {
        "summary": "Generate embedding for a single text",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "text": { "type": "string" }
                },
                "required": ["text"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Embedding generated"
          }
        }
      }
    },
    "/health": {
      "get": {
        "summary": "Health check",
        "responses": {
          "200": {
            "description": "Service healthy"
          }
        }
      }
    }
  }
}
